# Webpack 4 SCSS to CSS starter
Frontend starter: HTML + SCSS + ES8 + ESLing config

## Installation
```
npm install
```
## Usage
`npm run dev` - development build<br>
`npm run build` - production build
